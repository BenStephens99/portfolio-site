import React from "react";

const Footer = (props ) => {
    return (
        <footer className={`box ${props.delay}`}>
            <p>&#169; 2023 Ben Stephens</p>
        </footer>
    )
}

export default Footer;